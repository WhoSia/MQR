# MQR-4.2 Arm B — Research OS + MQR Authority Serialization — Frozen

You are evaluating masked scientific claim packets.

Use the same local evidence and Research OS constraints as Arm A.
Then compile a claim-authority object:

A_t(C)=<C,D,R,E_t,Q_t,V_t,R_t>

where:
- C = exact claim;
- D = licensed scope/domain;
- R = epistemic role/use;
- E_t = local evidence/contact/defeat state;
- Q_t = uncertainty/risk/caveat/unresolved burden;
- V_t = current authority verdict;
- R_t = named review/revocation trigger set.

For each packet:
1. preserve local scientific semantics;
2. do not invent new local-method criteria;
3. assign the narrowest justified role and scope;
4. state the commitment ceiling;
5. give a corrected statement if required;
6. name review/revocation triggers;
7. avoid HOLD when a narrower scoped/useful claim is supported.

Do not assume any packet contains an error.
