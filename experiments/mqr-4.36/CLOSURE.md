# MQR-4.36 — Fresh Naturalistic Fiber Attack Closure

Status: CLOSED / FRESH NU3 NATURALISTIC SUFFICIENCY FAIL / DECISION-RELATIVE CORE SUFFICIENCY NOT DEFEATED / FINITE INTERNAL RESIDUE ZERO / OPEN-WORLD RESIDUE NOT ELIMINATED / REAL-LANGUAGE V0.4 PROOF LANE LIVE

## Stage

MQR-4.36 — Fresh Naturalistic Fiber Attack, Pre-Registered Probe Algebra, Cross-Domain Mechanism Blinding, NU3 Collision Search, Decision-Relative Sufficiency & Whether Open-World Residue Can Be Bounded without Closing the World

## Intake and literature custody

The newly supplied paper was normalized and moved out of 00_INTAKE:

`Rubenstein et al. (2017) — Causal Consistency of Structural Equation Models.pdf`

The old intake filename `camera_ready_uai2017.pdf` no longer appears in Drive search.

The normalized paper is in the canonical paper parent used by the MQR literature collection.

## Frozen constitution

Primary preseal:
`903374ad64ee3c38f23598b84624166da14f718d`

Frozen candidate manifest:
`0167f4be300b405342451f90db4fdbc8ee1729c4`

Frozen atomic probe registry:
`b060cdb8ee2177f6e9396a322118b2f4a6b9532b`

NU3 was inherited unchanged from MQR-4.35:

```text
NU3 =
  MU
  + MAP_ALGEBRA
  + INTERVENTION_ALIGNMENT
  + SENSITIVITY_SCOPE
```

No coordinate was added after behavioral reveal.

## Fresh naturalistic world contact

Canonical first successful world-contact run:

`36143991326`

Evidence head:

`2ae08754422ed01fc72654599f952b86930afc4a`

All 12 frozen package-version installations succeeded.

Canonical artifact digest:

`sha256:638b6785870af428393050bbbb4c9818109c6011c852eb2bd413343cac5acd30`

Raw concatenated TSV:
- rows = 132
- SHA-256 = `a04fab623a7302f55e41e9a08d685047c3232b77a2b008888b3feba7254b342e`

## Fresh fiber adjudication

```text
JSCHEMA-001 -> F-R1 -> E1_QUOTIENT_ONLY
OASV-001    -> F-R1 -> E0_FULL

UCD2-001    -> F-R2 -> E1_QUOTIENT_ONLY
WCWIDTH-001 -> F-R2 -> E1_QUOTIENT_ONLY
```

F-R1 was frozen to the exact same NU3 key for both cases.

Yet:

```text
E_P(JSCHEMA-001) != E_P(OASV-001)
```

Therefore:

```text
NU3_NATURALISTIC_SUFFICIENCY = FAIL
```

This is a fresh naturalistic same-NU3 opposite-reach collision, not a forcing-world-only defeat.

## Identification state

The sealed corpus had:
- fresh N = 4;
- domain families = 2;
- lineage kinds = 1;
- RESEARCH_LAB N = 0.

Therefore positive naturalistic identification was already impossible under the preregistered constitution.

Result:

```text
NU3_NATURALISTIC_IDENTIFICATION = HOLD
POST_REVEAL_RESCUE = FORBIDDEN
```

The fresh collision strengthens the negative side but does not manufacture a positive identification claim.

## Decision-relative sufficiency

Fine decision D36 is injective over E0/E1/E2/E3.

F-R1 therefore also contains a D36 collision.

But the preregistered coarser decision:

```text
D36_CORE:
  E0_FULL or E1_QUOTIENT_ONLY -> ALLOW_CORE_REUSE
  E2_NONE -> REJECT_CORE_REUSE
  E3_UNRESOLVED -> HOLD
```

maps both F-R1 worlds to the same action.

Therefore on the admitted fresh corpus:

```text
NU3 exact empirical-reach sufficiency = FAIL
NU3 fine D36 sufficiency = FAIL
NU3 D36_CORE decision-relative sufficiency = NOT_DEFEATED
```

This does not establish universal D36_CORE sufficiency.

It demonstrates that a representation may be inadequate for exact empirical reach while remaining adequate for a deliberately coarser frozen decision.

## Finite residue theorem

The preregistered probe class is finite by construction.

All 132 declared atomic execution rows completed.

Unresolved rows:

`0`

Therefore:

```text
INTERNAL_RESIDUE_WITHIN_DECLARED_PROBE_CLASS = ZERO
```

This is an earned finite result.

It is quantified only over the declared class.

It does not imply:

```text
OPEN_WORLD_RESIDUE = ZERO
```

Instead:

```text
OPEN_WORLD_RESIDUE = NOT_ELIMINATED
```

## Finite-residue answer

Question:

> Can residue be finitely bounded for an explicitly declared intervention/probe class without closing the world?

MQR-4.36 answer:

```text
YES,
if the bound is explicitly internal to a finite or finitely normalized probe class,
the enumerator is complete for that class,
decision relevance is declared,
every relevant probe is executed or mechanically discharged,
and missingness cannot become PASS.

NO,
if the same certificate is interpreted as completeness of future scientific intervention,
measurement vocabulary,
instrumentation,
observable distinctions,
or ontology.
```

The key distinction is quantifier scope, not confidence level.

## Real-Language v0.4

Real-Language now has a live formal/empirical proof-boundary lane.

Canonical Rust compiler:
`language/real`

Formal backend:
Lean 4.

Pinned Lean toolchain:
`leanprover/lean4:v4.34.0`

Canonical proof-boundary run:
`36146576232`

Evidence head:
`65e1932ea48c013d59384c8b2f7e1d778865c061`

Independent checker stack:
- lean4export = `076e8e57707e813375e8f9da8bf989799ace9680`
- nanoda_lib = `68d5ca9db226849b41a6fff59d796ff19d0a8840`

Target theorem closure:
- `MQR.zeroInternalResidueOfComplete`
- `MQR.extensionContainsUncoveredProbe`
- `MQR.checkedProofCannotRaiseHoldToPass`

Lean kernel result:

```text
REAL_LANGUAGE_V04_LEAN_KERNEL=PASS
REAL_LANGUAGE_V04_AXIOM_ANCESTRY=EMPTY
```

Independent replay:

```text
REAL_LANGUAGE_V04_NANODA_INDEPENDENT_CHECKER=PASS
```

Current Real-Language CI:
`36147222451`

Conclusion:
SUCCESS.

## What the proof assistant proves

The formal layer establishes derivability of bounded statements such as:

```text
complete coverage of declared Fin n probe class
-> zero internal residue in that declared class
```

and:

```text
a strict syntactic extension may contain a probe uncovered by the old Tested predicate
```

and:

```text
formal CHECKED proof cannot raise empirical HOLD to PASS under the no-laundering relation
```

It does not establish:

```text
World |= empirical premises
World |= probe class is adequate
World |= all future distinctions are represented
OPEN_WORLD_RESIDUE = ZERO
```

Therefore:

```text
KERNEL_VERIFIED != WORLD_VERIFIED
```

## Proof-Carrying Realist Authority

The working architecture is PCRA:

```text
PCRA =
  WORLD_RECEIPT
  + TYPED_PREMISE_AUTHORITY
  + FORMAL_DEPENDENCY_GRAPH
  + KERNEL_CHECK
  + INDEPENDENT_RECHECK_WHEN_AVAILABLE
  + OPEN_WORLD_RESIDUE
```

The proof assistant checks the formal region.

Real-Language preserves the boundary and ancestry that constrain what the proof is scientifically allowed to mean.

## UV–Vis return

The earliest MQR instrument intuition survives in a sharper form:

```text
sample
 -> physical coupling
 -> detector
 -> digitization
 -> calibrated representation
 -> typed empirical premise
 -> Real-Language
 -> Lean
 -> scoped scientific consequence
```

Lean may verify:
- arithmetic;
- calibration equations as encoded;
- bounded algorithmic properties;
- logical consequences of explicit assumptions.

Lean cannot prove by theoremhood alone:
- that all relevant physical distinctions coupled into the detector;
- that calibration is adequate outside tested support;
- that the observation vocabulary is complete;
- that future instrumentation cannot split the current quotient.

## Re-Court results

The formal paradox court now contains at least these live failure problems:

1. Axiom-Laundering Paradox
2. Probe-Closure Paradox
3. Kernel–World Cut Problem
4. Receipt Reification Problem
5. Counterexample-Generator Exteriority Problem
6. Model-Soundness / Kernel-Soundness Split
7. Decision-Relative Closure Problem
8. Proof-Carrying Measurement Regress
9. Theorem-Prover Realism Trap
10. Open-World Residue Bounding Question
11. Decision-Compression Sanctuary
12. Finite Exhaustion / Adequacy Gap
13. Checker-Plurality Independence Problem
14. Premise-Granularity Collapse
15. Boundary-Migration Problem

The method remains:

```text
ARGUMENT
 -> NAME THE FAILURE PROBLEM
 -> CONSTRUCT COUNTERMODEL / BOUNDARY
 -> FORMALIZE SURVIVING CLAIM
 -> ATTACK FORMALIZATION
 -> FREEZE RESIDUE
 -> RE-COURT
```

Named paradoxes receive no authority by rhetorical force.

## Strongest earned result

MQR-4.36 establishes simultaneously:

```text
NU3_NATURALISTIC_SUFFICIENCY = FAIL
D36_CORE_DECISION_SUFFICIENCY = NOT_DEFEATED
INTERNAL_RESIDUE_WITHIN_DECLARED_PROBE_CLASS = ZERO
OPEN_WORLD_RESIDUE = NOT_ELIMINATED
REAL_LANGUAGE_V04_PROOF_BOUNDARY = LIVE
FORMAL_CERTAINTY_CANNOT_LAUNDER_EMPIRICAL_UNCERTAINTY
```

The combination matters.

A finite formal closure can be real without being universal.
A decision can be adequately supported without the representation being empirically complete.
A checked theorem can be certain relative to premises whose world entitlement remains defeasible.

## Philosophical update

Open-world realism does not require refusing all closure.

It requires typing closure by its quantifier and preserving a live route by which the surrounding world can invalidate the declared class, representation or premise boundary.

Thus the target is not:

```text
NO CLOSURE
```

and not:

```text
TOTAL CLOSURE
```

but:

```text
SCOPED FORMAL CLOSURE
+
EXPLICIT WORLD-CONTACT FRONTIER
+
REOPENABILITY
```

## Verdict

FRESH-NATURALISTIC-NU3-COLLISION=PASS /
NU3-NATURALISTIC-SUFFICIENCY=FAIL /
NU3-NATURALISTIC-IDENTIFICATION=HOLD /
D36-FINE-SUFFICIENCY=FAIL /
D36-CORE-SUFFICIENCY=NOT-DEFEATED /
DECLARED-ATOMIC-PROBES=132 /
UNRESOLVED-ATOMIC-PROBES=0 /
FINITE-INTERNAL-RESIDUE=ZERO /
OPEN-WORLD-RESIDUE=NOT-ELIMINATED /
LEAN-4.34.0-KERNEL=PASS /
TARGET-AXIOM-ANCESTRY=EMPTY /
NANODA-INDEPENDENT-RECHECK=PASS /
REAL-LANGUAGE-V0.4-PROOF-LANE=LIVE /
PCRA=WORKING-ARCHITECTURE /
KERNEL-VERIFIED-NEQ-WORLD-VERIFIED /
POST-REVEAL-REPAIR=FORBIDDEN /
GENERATION-IV-CONTINUES.
